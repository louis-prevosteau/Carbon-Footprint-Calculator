import React from 'react';

const SecondaryResidence = ({ people, handleDataToHolidays }: { people: number, handleDataToHolidays: any }) => {
    return (
        <div>
            
        </div>
    );
};

export default SecondaryResidence;